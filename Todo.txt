make a get onwall timers from physics (in ms)
physics make wall bouce if over treshold (set treshold and bounce amount)
move fps text to the resorce manager

test timers
Test if the keys work (maybe set bg color depending on key and its state)


